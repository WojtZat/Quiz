<hibernate-configuration>


    <session-factory>

        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">org.sqlite.JDBC</property>
        <!--<property name="connection.url">jdbc:sqlite:C:\Users\User\Desktop\Quiz\Quiz\src\main\resources\quiz.db</property>-->
        <property name="connection.url">jdbc:sqlite::resource:quiz.db</property>
        <!--<property name="connection.username">hbstudent</property>-->
        <!--<property name="connection.password">hbstudent</property>-->

        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="connection.pool_size">1</property>

        <!--<property name="hbm2ddl.import_files">import.sql</property>-->

        <property name="javax.persistence.schema-generation.database.action">create</property>
        <property name="javax.persistence.schema-generation.create-source">script</property>
        <property name="javax.persistence.schema-generation.create-script-source">import.sql</property>

        <!-- Select our SQL dialect -->
        <property name="dialect">org.hibernate.dialect.SQLiteDialect</property>

        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Set the current session context -->
        <property name="current_session_context_class">thread</property>

        <!--<property name="hibernate.hbm2ddl.auto">create</property>-->

    </session-factory>

</hibernate-configuration>
